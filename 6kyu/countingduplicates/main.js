/*   Count the number of Duplicates
Write a function that will return the count of distinct case-insensitive alphabetic characters and numeric digits that occur more than once in the input string. The input string can be assumed to contain only alphabets (both uppercase and lowercase) and numeric digits. */
//P parameter being taken in a "string" of characters and integers 
//R return the "count " of the repeated character & number for repetition
//E  "aabbcde" -> 2 # 'a' and 'b'
//P create a function that will take in a string and evaluate which character is seen most throughout:: return character and count # as a digit:: create a count variable


// function duplicateCount(text){
//   //...
//   let text = text.split('')
//   let count = ''

//  for(i)
// }
